import React from "react";
import TodoContainer from "./components/TodoContainer";

const page = async () => {
    return (
        <main>
            <TodoContainer />
        </main>
    );
};

export default page;
